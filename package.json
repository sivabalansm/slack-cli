{
  "name": "slack-cli",
  "version": "1.0.0",
  "description": "Slack CLI tool for sending/reading/searching messages as your user",
  "bin": {
    "slack": "./slack"
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
